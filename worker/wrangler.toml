name = "afterset-capture"
main = "src/index.ts"
compatibility_date = "2024-12-01"
workers_dev = false

routes = [
  { pattern = "afterset.net/c/*", zone_name = "afterset.net" }
]

[[r2_buckets]]
binding = "PAGES"
bucket_name = "afterset-capture-pages"

[vars]
ENVIRONMENT = "production"
